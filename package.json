{
  "name": "typer-server",
  "version": "2.0.0",
  "description": "Remote typer bot server — zero dependencies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.x"
  }
}